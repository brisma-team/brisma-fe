import { ImageCircleCheckGreen } from "@/helpers/imagesUrl";
import Image from "next/image";

const ButtonSave = () => {
  return <Image src={ImageCircleCheckGreen} alt="" width={22} height={22} />;
};

export default ButtonSave;
