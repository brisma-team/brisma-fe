import { IconRetry } from "@/components/icons";

const RetryButton = () => {
  return (
    <div className="rounded-full w-5 h-5 flex items-center justify-center p-1">
      <IconRetry size="medium" />
    </div>
  );
};

export default RetryButton;
